const styles = (theme)=>({
    taskboard:{
        display: 'flex',
        alignItems: 'center',
    },
    shape:{
        backgroundColor: theme.color.primary,
        color: 'white',
        borderColor: '#ccc',
        padding: 20,
        margin:10,
        borderRadius: 4
    },
})

export default styles